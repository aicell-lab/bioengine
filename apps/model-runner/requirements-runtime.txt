bioimageio.core==0.11.0
careamics==0.0.16
cellpose==4.2.1.1
# micro-SAM vit_t artifacts import mobile_sam; segment-anything has no vit_t.
git+https://github.com/ChaoningZhang/MobileSAM.git@f706ad9c4eb7f219c00d9050e46328518ffb65d2
git+https://github.com/facebookresearch/dinov3.git@346f38fee679c56a6888f91c51670fae61d364e0
# NOT the PyPI segment-anything==1.0: that snapshot predates the fix to
# ResizeLongestSide.apply_image_torch, which reads shape[0], shape[1] instead of
# shape[2], shape[3] and so resizes BCHW input by its batch/channel dims.
# segment-anything-py==1.0.1 carries the same fixed source, but do NOT swap it in:
# it ships the segment_anything package under a different distribution name, so
# cellpose's bare segment_anything requirement still drags in the broken 1.0 wheel
# and whichever installs last overwrites the other's files. This URL keeps the
# distribution name, so there is only ever one copy.
git+https://github.com/facebookresearch/segment-anything.git@dca509fe793f601edb92606367a655c15ac00fdf
numpy==1.26.4
nvidia-ml-py==12.555.43
onnxruntime==1.20.1
psutil==6.1.1
scikit-image==0.25.2
stardist==0.9.1
tensorflow==2.16.1
timm==1.0.27
torch==2.7.1
torchvision==0.22.1
xarray==2025.1.2
