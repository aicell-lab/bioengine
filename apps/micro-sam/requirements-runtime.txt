# micro-sam 1.8.5 is the broadest-compatible pin (Requires-Python >=3.10); the
# full pip install pulls torch-em / segment-anything / bioimage-cpp, so the old
# conda-only nifty/vigra/z5py blocker is gone. onnx(runtime) is only for the
# in-browser prompt-decoder export (get_onnx_model).
micro-sam==1.8.5
# Pin bioimageio.core to 0.9.4 (→ bioimageio.spec 0.5.5.6). micro-sam 1.8.5's
# export_sam_model passes plain str/Path for cover/documentation, which newer
# bioimageio.spec (0.5.12, pulled by the default core 0.11) rejects — it now
# requires FileDescr. 0.5.5.6 still accepts str, so the BioImage.IO export works.
# Serving/training don't import bioimageio.core, so this only affects export.
bioimageio.core==0.9.4
segment-anything==1.0
torch==2.7.1
torchvision==0.22.1
onnx==1.17.0
onnxruntime==1.21.0
imageio>=2.37.0
# numpy stays at 2.x: micro-sam → python-elf (>=0.9, the AIS decoder) hard-requires
# numpy>=2, and no numpy<2 python-elf exists on PyPI. The bioengine worker's
# ProxyDeployment runs numpy 1.x and cannot unpickle numpy-2.x arrays over the Ray
# hop, so this app returns arrays as hypha ndarray wire-dicts (bytes) instead of raw
# ndarrays — see RuntimeApp._nd. Leave numpy unpinned so elf resolves it.
