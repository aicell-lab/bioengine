# micro-sam 1.8.5 is the broadest-compatible pin (Requires-Python >=3.10); the
# full pip install pulls torch-em / segment-anything / bioimage-cpp, so the old
# conda-only nifty/vigra/z5py blocker is gone. onnx(runtime) is only for the
# in-browser prompt-decoder export (get_onnx_model).
micro-sam==1.8.5
segment-anything==1.0
torch==2.7.1
torchvision==0.22.1
onnx==1.17.0
onnxruntime==1.21.0
imageio>=2.37.0
# Pin numpy to the 1.x the worker/proxy ships: the app's returned arrays are
# pickled by Ray and unpickled in the ProxyDeployment, and numpy 2.x pickles
# reference numpy._core which numpy 1.x cannot import (ModuleNotFoundError:
# numpy._core.numeric). Must match across the replica↔proxy boundary.
numpy==1.26.4
